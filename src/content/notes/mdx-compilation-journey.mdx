---
title: 'How MDX gets compiled to JSX'
subtitle: 'A journey through AST land'
description: 'Shows the steps taken to compile MDX to JSX'
pubDate: '07/11/22'
tags: ['ASTs', 'MDX']
keywords: 'MDX, ASTs, Abstract Syntax Tree, JSX, compilation'
---

import { Image } from 'astro:assets';
import diagram from 'assets/MDX-compilation.svg';

Note to self: these are the steps to compile from MDX to JSX. Use `remark`
to transform the MDAST and `rehype` to transform the HAST.

<Image src={diagram} alt="Diagram of how MDX gets compiled to JSX" />
